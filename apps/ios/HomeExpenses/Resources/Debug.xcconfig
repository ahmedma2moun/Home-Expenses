// Local development — points at your machine (simulator). For a device, point this at a Preview
// deployment instead (https://..., which needs no ATS exception — see Info.plist's
// NSAppTransportSecurity, scoped to localhost only).
API_BASE_URL = http:/$()/localhost:3000
